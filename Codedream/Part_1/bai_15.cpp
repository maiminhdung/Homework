#include <iostream>
#include <cmath>

using namespace std;
int main() {
	int a, b, c, d;
	cout << "Nhap gia tri a, b, c, d: ";
	cin >> a >> b >> c >> d;
	
	cout << "Ket qua la: " << pow(a,b) + pow(c,d);
}
